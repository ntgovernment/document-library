{
  "name": "document-library",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --config vite.collection.config.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
